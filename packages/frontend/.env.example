# Development environment variables

# Enable mock wallet for development (set to 'true' to use mock balances)
VITE_USE_MOCK_WALLET=true

# API base URLs (if needed)
# VITE_API_BASE_URL=http://localhost:3000
